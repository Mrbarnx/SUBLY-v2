import { useMemo, useReducer, type ReactNode } from 'react'
import type { CartItem, Product } from '../../types'
import { CartContext, type CartContextValue } from './cartContext'
interface CartState { items:CartItem[] }
type Action={type:'add';product:Product;quantity:number}|{type:'update';productId:string;quantity:number}|{type:'remove';productId:string}|{type:'clear'}
function reducer(state:CartState,action:Action):CartState{switch(action.type){case'add':{const existing=state.items.find(item=>item.product.id===action.product.id);return {items:existing?state.items.map(item=>item.product.id===action.product.id?{...item,quantity:item.quantity+action.quantity}:item):[...state.items,{product:action.product,quantity:action.quantity}]}}case'update':return {items:action.quantity>0?state.items.map(item=>item.product.id===action.productId?{...item,quantity:action.quantity}:item):state.items.filter(item=>item.product.id!==action.productId)};case'remove':return {items:state.items.filter(item=>item.product.id!==action.productId)};case'clear':return {items:[]}}}
export function CartProvider({children}:{children:ReactNode}){const [state,dispatch]=useReducer(reducer,{items:[]});const value=useMemo<CartContextValue>(()=>({...state,itemCount:state.items.reduce((sum,item)=>sum+item.quantity,0),subtotal:state.items.reduce((sum,item)=>sum+item.product.price*item.quantity,0),addItem:(product,quantity=1)=>dispatch({type:'add',product,quantity:Math.max(1,quantity)}),updateQuantity:(productId,quantity)=>dispatch({type:'update',productId,quantity}),removeItem:productId=>dispatch({type:'remove',productId}),clear:()=>dispatch({type:'clear'})}),[state]);return <CartContext.Provider value={value}>{children}</CartContext.Provider>}

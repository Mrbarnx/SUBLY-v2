import { forwardRef, useId, type InputHTMLAttributes, type ReactNode } from 'react'
import { cn } from '../../lib/cn'
export interface InputProps extends InputHTMLAttributes<HTMLInputElement> { label: string; error?: string; hint?: ReactNode; success?: boolean }
export const Input = forwardRef<HTMLInputElement, InputProps>(function Input({ className, label, error, hint, success, id: givenId, ...props }, ref) { const generatedId=useId(); const id=givenId??generatedId; const helpId=`${id}-help`; return <div className="grid gap-2"><label className="text-sm font-semibold text-navy" htmlFor={id}>{label}</label><input ref={ref} id={id} aria-invalid={!!error} aria-describedby={(error||hint)?helpId:undefined} className={cn('min-h-11 w-full rounded-card border bg-white px-3 py-2 text-navy transition-colors duration-200 placeholder:text-slate-400 disabled:cursor-not-allowed disabled:bg-slate-100 disabled:text-slate-500', error?'border-danger focus-visible:outline-danger':success?'border-teal':'border-border', className)} {...props}/>{(error||hint)&&<p id={helpId} className={cn('text-sm',error?'text-danger':'text-slate-600')}>{error??hint}</p>}</div> })

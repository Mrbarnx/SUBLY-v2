import { useState, type FormEvent } from 'react'
export function Newsletter(){const [sent,setSent]=useState(false);function submit(event:FormEvent){event.preventDefault();setSent(true)}return <section className="rounded-card border border-blue-100 bg-blue-50 p-5 sm:flex sm:items-center sm:justify-between sm:gap-8"><div><h2 className="text-xl font-bold">Get product updates</h2><p className="mt-1 text-sm text-slate-600">New tools, exclusive offers and useful tips. No spam.</p></div><form onSubmit={submit} className="mt-4 flex min-w-0 flex-1 gap-2 sm:mt-0 sm:max-w-xl"><label className="sr-only" htmlFor="newsletter-email">Email address</label><input id="newsletter-email" required type="email" placeholder="Enter your email" className="min-w-0 flex-1 rounded-card border border-border bg-white px-3"/><button className="rounded-card bg-primary px-4 py-3 font-semibold text-white">Subscribe</button></form>{sent&&<p className="mt-2 text-sm text-emerald-700 sm:hidden">Thanks for subscribing.</p>}</section>}

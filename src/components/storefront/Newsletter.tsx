import { useState, type FormEvent } from 'react'
import { Icon } from '../ui/Icon'
export function Newsletter(){const [sent,setSent]=useState(false);function submit(event:FormEvent){event.preventDefault();setSent(true)}return <section className="border border-blue-100 bg-blue-50/70 p-5 sm:flex sm:items-center sm:justify-between sm:gap-8"><div className="flex gap-4"><span className="hidden size-11 place-items-center rounded-card bg-white text-primary shadow-sm sm:grid"><Icon name="mail"/></span><div><h2 className="text-xl font-bold">Get product updates</h2><p className="mt-1 text-sm text-slate-600">New tools, exclusive offers and useful tips. No spam.</p></div></div><form onSubmit={submit} className="mt-4 flex min-w-0 flex-col gap-2 sm:mt-0 sm:max-w-xl sm:flex-1 sm:flex-row"><label className="sr-only" htmlFor="newsletter-email">Email address</label><input id="newsletter-email" required type="email" placeholder="Enter your email" className="min-h-11 min-w-0 flex-1 rounded-card border border-slate-200 bg-white px-3"/><button className="rounded-card bg-primary px-5 py-3 font-semibold text-white">Subscribe</button></form>{sent&&<p role="status" className="mt-2 text-sm text-emerald-700 sm:hidden">Thanks for subscribing.</p>}</section>}

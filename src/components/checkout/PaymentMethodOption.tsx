import type { PaymentMethod } from '../../types'
import { Icon, type IconName } from '../ui/Icon'
export function PaymentMethodOption({value,selected,onChange,icon,title,description,disabled=false}:{value:PaymentMethod;selected:boolean;onChange:(value:PaymentMethod)=>void;icon:IconName;title:string;description:string;disabled?:boolean}){return <label className={"relative flex min-h-28 cursor-pointer flex-col items-center justify-center rounded-card border p-3 text-center transition "+(selected?'border-primary bg-blue-50/60 ring-1 ring-primary':'border-slate-200 bg-white hover:border-blue-300')+(disabled?' cursor-not-allowed opacity-50':'')}><input className="sr-only" type="radio" name="paymentMethod" value={value} checked={selected} onChange={()=>onChange(value)} disabled={disabled}/><span className={"grid size-10 place-items-center rounded-card "+(selected?'bg-primary text-white':'bg-slate-100 text-slate-700')}><Icon name={icon}/></span><b className="mt-2 text-sm">{title}</b><span className="mt-0.5 text-xs text-slate-500">{description}</span>{selected&&<Icon name="circle-check" className="absolute right-2 top-2 size-4 fill-primary text-white"/>}</label>}

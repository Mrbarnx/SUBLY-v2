import type { PaymentMethod } from '../../types'
export function PaymentMethodOption({value,selected,onChange,icon,title,description,disabled=false}:{value:PaymentMethod;selected:boolean;onChange:(value:PaymentMethod)=>void;icon:string;title:string;description:string;disabled?:boolean}){return <label className={`relative flex min-h-24 cursor-pointer flex-col items-center justify-center rounded-card border p-3 text-center ${selected?'border-primary ring-1 ring-primary':'border-border'} ${disabled?'cursor-not-allowed opacity-50':''}`}><input className="sr-only" type="radio" name="paymentMethod" value={value} checked={selected} onChange={()=>onChange(value)} disabled={disabled}/><span className="text-2xl" aria-hidden="true">{icon}</span><b className="mt-1">{title}</b><span className="text-sm text-slate-500">{description}</span>{selected&&<span className="absolute right-2 top-2 text-primary">●</span>}</label>}

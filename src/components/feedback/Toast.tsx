import { useCallback, useMemo, useState, type ReactNode } from 'react'
import { ToastContext, type ToastInput, type ToastItem } from './toastContext'
export function ToastProvider({children}:{children:ReactNode}){const [toasts,setToasts]=useState<ToastItem[]>([]);const show=useCallback((toast:ToastInput)=>{const id=Date.now();setToasts(items=>[...items,{...toast,id}]);window.setTimeout(()=>setToasts(items=>items.filter(item=>item.id!==id)),4000)},[]);const value=useMemo(()=>show,[show]);return <ToastContext.Provider value={value}>{children}<div className="fixed bottom-4 right-4 z-50 grid w-[calc(100%-2rem)] max-w-sm gap-2" aria-live="polite" aria-atomic="true">{toasts.map(toast=><div key={toast.id} role={toast.tone==='error'?'alert':'status'} className={`rounded-card border bg-white p-4 shadow-dialog ${toast.tone==='error'?'border-red-200':toast.tone==='success'?'border-teal':'border-border'}`}>{toast.message}</div>)}</div></ToastContext.Provider>}

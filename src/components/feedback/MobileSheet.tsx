import { useEffect, useRef, type ReactNode } from 'react'
import { Icon } from '../ui/Icon'
export function MobileSheet({open,onClose,title,children}:{open:boolean;onClose:()=>void;title:string;children:ReactNode}){const closeRef=useRef<HTMLButtonElement>(null);useEffect(()=>{if(!open)return;const previous=document.body.style.overflow;document.body.style.overflow='hidden';closeRef.current?.focus();const close=(event:KeyboardEvent)=>{if(event.key==='Escape')onClose()};window.addEventListener('keydown',close);return()=>{document.body.style.overflow=previous;window.removeEventListener('keydown',close)}},[open,onClose]);if(!open)return null;return <div className="fixed inset-0 z-50 md:hidden"><button className="absolute inset-0 bg-navy/65" onClick={onClose} aria-label="Close menu backdrop"/><section role="dialog" aria-modal="true" aria-labelledby="sheet-title" className="absolute inset-y-0 right-0 w-[min(22rem,92vw)] overflow-y-auto border-l border-slate-200 bg-white p-5 shadow-dialog"><div className="flex items-center justify-between gap-4"><h2 id="sheet-title" className="text-xl font-bold">{title}</h2><button ref={closeRef} onClick={onClose} aria-label="Close menu" className="grid size-10 place-items-center rounded-card border border-slate-200"><Icon name="x"/></button></div><div className="mt-5">{children}</div></section></div>}

import { useEffect, type ReactNode } from 'react'
export function MobileSheet({open,onClose,title,children}:{open:boolean;onClose:()=>void;title:string;children:ReactNode}){useEffect(()=>{if(!open)return;const previous=document.body.style.overflow;document.body.style.overflow='hidden';const close=(event:KeyboardEvent)=>{if(event.key==='Escape')onClose()};window.addEventListener('keydown',close);return()=>{document.body.style.overflow=previous;window.removeEventListener('keydown',close)}},[open,onClose]);if(!open)return null;return <div className="fixed inset-0 z-50 md:hidden"><button className="absolute inset-0 bg-navy/60" onClick={onClose} aria-label="Close menu"/><section role="dialog" aria-modal="true" aria-labelledby="sheet-title" className="absolute inset-y-0 right-0 w-[min(22rem,90vw)] overflow-y-auto bg-white p-5 shadow-dialog"><div className="flex justify-between"><h2 id="sheet-title" className="text-xl font-bold">{title}</h2><button onClick={onClose} aria-label="Close menu" className="size-10">×</button></div><div className="mt-5">{children}</div></section></div>}

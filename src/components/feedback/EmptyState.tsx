import type { ReactNode } from 'react'
import { Icon } from '../ui/Icon'
export function EmptyState({title,description,action}:{title:string;description:string;action?:ReactNode}){return <section className="border border-dashed border-slate-300 bg-slate-50/50 p-7 text-center" aria-labelledby="empty-title"><span className="mx-auto grid size-11 place-items-center rounded-card bg-white text-primary shadow-sm"><Icon name="package"/></span><h2 id="empty-title" className="mt-4 text-lg font-bold">{title}</h2><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-slate-600">{description}</p>{action&&<div className="mt-5">{action}</div>}</section>}

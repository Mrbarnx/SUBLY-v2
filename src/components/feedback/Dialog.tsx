import { useEffect, useRef, type ReactNode } from 'react'
import { Button } from '../ui/Button'
export interface DialogProps { open: boolean; onClose: () => void; title: string; description?: string; children?: ReactNode }
export function Dialog({open,onClose,title,description,children}:DialogProps){const dialogRef=useRef<HTMLDialogElement>(null);useEffect(()=>{const dialog=dialogRef.current;if(!dialog)return;if(open&&!dialog.open)dialog.showModal();if(!open&&dialog.open)dialog.close()},[open]);return <dialog ref={dialogRef} onClose={onClose} onCancel={onClose} aria-labelledby="dialog-title" aria-describedby={description?'dialog-description':undefined} className="m-auto w-[calc(100%-2rem)] max-w-lg rounded-card bg-white p-0 text-navy shadow-dialog backdrop:bg-navy/60"><div className="p-5 sm:p-6"><div className="flex items-start justify-between gap-4"><div><h2 id="dialog-title" className="text-xl font-bold">{title}</h2>{description&&<p id="dialog-description" className="mt-2 text-slate-600">{description}</p>}</div><Button variant="ghost" className="min-h-9 shrink-0 px-3" onClick={onClose} aria-label="Close dialog">Close</Button></div>{children&&<div className="mt-6">{children}</div>}</div></dialog>}

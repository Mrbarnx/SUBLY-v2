import type { ReactNode } from 'react'
import { Container } from '../layout/Container'
export function AuthShell({eyebrow,title,description,children,asideTitle='Everything you need, securely in one place.',asideItems=['Verified digital access','Clear order tracking','Support when you need it'] }:{eyebrow?:string;title:string;description:string;children:ReactNode;asideTitle?:string;asideItems?:string[]}){return <Container className="grid flex-1 items-stretch gap-0 px-0 md:grid-cols-[minmax(18rem,.85fr)_minmax(0,1.15fr)] lg:max-w-6xl lg:py-10"><aside className="hidden bg-gradient-to-br from-navy via-blue-950 to-primary p-10 text-white md:flex md:flex-col md:justify-center lg:rounded-l-card"><p className="text-sm font-bold uppercase text-soft-teal">{eyebrow??'Welcome to Subly'}</p><h2 className="mt-5 text-3xl font-extrabold leading-tight">{asideTitle}</h2><div className="mt-8 grid gap-5">{asideItems.map(item=><p key={item} className="flex gap-3"><span className="text-soft-teal">✓</span><b>{item}</b></p>)}</div><p className="mt-10 border-t border-white/20 pt-6 text-sm text-blue-100">Your information stays in this frontend demo and is never sent to an authentication provider.</p></aside><section className="flex items-center p-5 sm:p-8 lg:rounded-r-card lg:border lg:border-l-0 lg:border-border"><div className="mx-auto w-full max-w-xl"><h1 className="text-3xl font-extrabold leading-tight">{title}</h1><p className="mt-2 leading-6 text-slate-600">{description}</p><div className="mt-7">{children}</div></div></section></Container>}

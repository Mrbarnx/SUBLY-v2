export function PasswordRequirements({password,confirmation}:{password:string;confirmation?:string}){const rules=[['At least 8 characters',password.length>=8],['One uppercase letter',/[A-Z]/.test(password)],['One number',/\d/.test(password)],...(confirmation===undefined?[]:[['Passwords match',password.length>0&&password===confirmation] as [string,boolean]])];return <ul className="grid gap-2 text-sm sm:grid-cols-2">{rules.map(([label,valid])=><li key={label as string} className={valid?'text-emerald-700':'text-slate-500'}>{valid?'●':'○'} {label}</li>)}</ul>}

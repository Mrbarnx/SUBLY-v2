import { useState } from 'react'
import type { UseFormRegisterReturn } from 'react-hook-form'
export function PasswordField({label,placeholder='Enter your password',registration,error}:{label:string;placeholder?:string;registration:UseFormRegisterReturn;error?:string}){const [visible,setVisible]=useState(false);return <div><label className="text-sm font-semibold" htmlFor={registration.name}>{label}</label><div className="relative mt-2"><input id={registration.name} type={visible?'text':'password'} placeholder={placeholder} {...registration} aria-invalid={!!error} className="min-h-11 w-full rounded-card border border-border px-3 pr-14"/><button type="button" onClick={()=>setVisible(v=>!v)} className="absolute inset-y-0 right-0 px-4 text-sm text-slate-500" aria-label={`${visible?'Hide':'Show'} ${label.toLowerCase()}`}>{visible?'Hide':'Show'}</button></div>{error&&<p className="mt-1 text-sm text-danger">{error}</p>}</div>}

import { Icon, type IconName } from '../ui/Icon'
export function StatCard({icon,label,value,detail}:{icon:IconName;label:string;value:string;detail?:string}){return <article className="border border-slate-200 bg-white p-4 shadow-sm sm:p-5"><div className="flex items-start gap-3"><span className="grid size-10 shrink-0 place-items-center rounded-card bg-blue-50 text-primary"><Icon name={icon}/></span><div className="min-w-0"><p className="text-xs font-medium text-slate-500">{label}</p><strong className="mt-1 block break-words text-xl sm:text-2xl">{value}</strong>{detail&&<p className="mt-1 text-xs font-medium text-primary">{detail}</p>}</div></div></article>}

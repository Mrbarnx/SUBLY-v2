import { useState, type FormEvent } from 'react'
import { NavLink, Outlet, useNavigate } from 'react-router-dom'
import { Container } from './Container'
import { SublyLogo } from '../storefront/SublyLogo'
import { Footer } from '../storefront/Footer'
import { useCart } from '../../features/cart'
export function PublicLayout(){const [open,setOpen]=useState(false);const [search,setSearch]=useState('');const navigate=useNavigate();const {itemCount}=useCart();function submit(e:FormEvent){e.preventDefault();navigate(`/shop?q=${encodeURIComponent(search)}`);setOpen(false)}return <div className="min-h-screen"><header className="sticky top-0 z-40 border-b border-border bg-white/95 backdrop-blur"><Container className="flex min-h-16 items-center justify-between gap-5"><SublyLogo/><nav aria-label="Primary" className="hidden items-center gap-6 text-sm font-semibold md:flex"><NavLink to="/shop">Shop</NavLink><NavLink to="/how-it-works">How It Works</NavLink><NavLink to="/shop">Categories</NavLink></nav><form onSubmit={submit} className="hidden min-w-0 flex-1 lg:flex"><label className="sr-only" htmlFor="site-search">Search products</label><input id="site-search" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search tools, categories..." className="w-full rounded-card border border-border px-4 py-2"/></form><div className="hidden items-center gap-4 text-sm md:flex"><NavLink to="/login">Log in</NavLink><NavLink to="/signup" className="rounded-card bg-primary px-3 py-2 font-semibold text-white">Create account</NavLink><NavLink to="/cart" className="relative text-xl" aria-label={`Cart with ${itemCount} items`}>🛒{itemCount>0&&<span className="absolute -right-2 -top-2 rounded-full bg-primary px-1 text-[10px] text-white">{itemCount}</span>}</NavLink></div><button className="rounded-card border border-border px-3 py-2 md:hidden" onClick={()=>setOpen(v=>!v)} aria-expanded={open} aria-controls="mobile-menu">Menu</button></Container>{open&&<div id="mobile-menu" className="border-t border-border bg-white p-4 md:hidden"><form onSubmit={submit} className="flex gap-2"><input value={search} onChange={e=>setSearch(e.target.value)} aria-label="Search products" placeholder="Search products..." className="min-w-0 flex-1 rounded-card border border-border px-3"/><button className="rounded-card bg-primary px-3 py-2 text-white">Search</button></form><nav className="mt-4 grid gap-1" aria-label="Mobile primary">{[['/shop','Shop'],['/how-it-works','How It Works'],['/cart',`Cart (${itemCount})`],['/login','Log in'],['/signup','Create account']].map(([to,label])=><NavLink onClick={()=>setOpen(false)} className="rounded-card px-3 py-2 font-semibold hover:bg-blue-50" key={to} to={to}>{label}</NavLink>)}</nav></div>}</header><main><Outlet/></main><Footer/></div>}

import { NavLink, Outlet } from 'react-router-dom'
import { Container } from './Container'
const links=[['/dashboard','Dashboard'],['/orders','Orders'],['/profile','Profile'],['/settings','Settings'],['/wallet','Wallet'],['/referrals','Referrals']]
export function AccountLayout(){return <div className="min-h-screen"><header className="border-b border-border bg-white"><Container className="flex min-h-16 items-center justify-between"><NavLink to="/" className="text-xl font-bold text-primary">Subly</NavLink><span className="text-sm font-semibold">Account</span></Container></header><Container className="grid gap-6 py-6 md:grid-cols-[12rem_1fr]"><nav aria-label="Account" className="flex gap-2 overflow-x-auto md:flex-col">{links.map(([to,label])=><NavLink key={to} to={to} className={({isActive})=>`whitespace-nowrap rounded-card px-3 py-2 text-sm font-semibold ${isActive?'bg-blue-50 text-primary':'hover:bg-white'}`}>{label}</NavLink>)}</nav><main className="min-w-0"><Outlet/></main></Container></div>}

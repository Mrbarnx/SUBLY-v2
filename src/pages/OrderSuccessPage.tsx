import { Link } from 'react-router-dom'
import { orderService } from '../services'
import { Container } from '../components/layout/Container'
import { EmptyState } from '../components/feedback'
import { OrderResultCard } from '../components/checkout/OrderResultCard'
export function OrderSuccessPage(){const order=orderService.getLastOrder();if(!order)return <Container className="py-16"><EmptyState title="No recent order found" description="This page needs a valid order from the current checkout session." action={<Link to="/shop" className="inline-block rounded-card bg-primary px-5 py-3 font-bold text-white">Continue Shopping</Link>}/></Container>;return <Container className="max-w-5xl py-10"><header className="text-center"><span className="mx-auto grid size-20 place-items-center rounded-full border-2 border-teal bg-teal/10 text-4xl text-emerald-600">✓</span><h1 className="mt-4 text-3xl font-extrabold">Order received</h1><p className="mt-2 text-slate-600">We’ve received your demo order information. Fulfilment begins only after payment confirmation.</p><div className="mx-auto mt-5 flex max-w-2xl flex-wrap justify-center gap-4 rounded-card border border-border bg-white p-4"><span className="text-slate-500">Order reference</span><b className="break-all">{order.reference}</b><button onClick={()=>void navigator.clipboard?.writeText(order.reference)} className="text-primary">Copy</button><time>{new Date(order.createdAt).toLocaleString()}</time></div></header><ol className="my-8 grid grid-cols-3 gap-3 text-center text-sm"><li className="text-emerald-700"><b>✓ Order received</b><p className="mt-1 text-slate-500">Details submitted.</p></li><li className="text-primary"><b>◉ Payment review</b><p className="mt-1 text-slate-500">Demo review pending.</p></li><li className="text-slate-500"><b>○ Delivery</b><p className="mt-1">After confirmation.</p></li></ol><p className="mb-5 rounded-card border border-blue-200 bg-blue-50 p-4 text-primary">ⓘ Order receipt is not payment confirmation. Do not make another payment based on this demo status.</p><OrderResultCard order={order} title="Items in your order"/><section className="mt-7"><h2 className="text-xl font-bold">What happens next?</h2><div className="mt-4 grid gap-5 sm:grid-cols-3">{['We review your payment choice','Fulfilment begins after confirmation','You receive order updates'].map((t,i)=><div key={t}><b>{i+1}. {t}</b><p className="mt-1 text-sm text-slate-600">This frontend demo records status without processing payment.</p></div>)}</div><div className="mt-7 flex flex-col justify-center gap-3 sm:flex-row"><Link to="/orders" className="rounded-card bg-primary px-5 py-3 text-center font-bold text-white">View Order Status</Link><Link to="/shop" className="rounded-card border border-primary px-5 py-3 text-center font-bold text-primary">Continue Shopping</Link><a href="mailto:support@subly.example" className="px-5 py-3 text-center font-bold text-primary">Contact Support</a></div></section></Container>}

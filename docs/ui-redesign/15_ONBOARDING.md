# Onboarding Redesign

## Route And File

Route: `/onboarding`

Primary file: `src/AuthScreens.jsx`

## Purpose

Onboarding welcomes a newly confirmed customer and captures lightweight preferences without blocking access to the store.

## Required Content

1. Personalized welcome heading.
2. Email update toggle.
3. WhatsApp alert toggle.
4. Category interest selection.
5. Start Browsing action.
6. Skip option.

## Copy Ready Prompt

Redesign only `/onboarding` in `src/AuthScreens.jsx`. Preserve the signed in user, preference state, profile update behavior, redirect target, and auth events. Use a friendly compact setup with clear toggles and category chips. Do not use a long questionnaire or block the user from browsing. Keep Start Browsing primary and provide a subtle Skip option. Use Subly colors and accessible controls. Run build and lint.
